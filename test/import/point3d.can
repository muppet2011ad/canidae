class Point3D {
    function __init__(x, y, z) {
        this.x = x;
        this.y = y;
        this.z = z;
    }

    function printme() {
        print "(" + str(this.x) + ", " + str(this.y) + ", " + str(this.z) + ")";
    }
}