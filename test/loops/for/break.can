{
    for let x = 0; x < 5; x++ do {
        if x == 3 then break;
        print x;
    }
    let x = 0;
    for ; x < 5; x++ do {
        for let y = 0; y < 5; y++ do {
            if y == 3 then break;
        }
    }
    print x;
}